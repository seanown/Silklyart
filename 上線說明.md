# SILKY ART 靜態展示站 — 上線說明

> **語言結構（2026-09-06 改版）**：**英文為主站**——根目錄就是英文版（`index.html` 等七頁）；**中文收進 `zh/`**（`zh/index.html` 等七頁）。兩邊共用 `assets/`，頂部有「中文 / EN」切換。自訂網域 `silklyart.com` 開啟後直接給英文主站，`/zh/` 為中文版。

> 本資料夾是 **純靜態展示站**（HTML + CSS + 極少量 JS），無後端、無資料庫。
> 目的：先上線收藝術家投件、講清楚定位，**不顯價格**、不碰後端。
> ✅ **Mondo 風藝術指導 v1 已於 2026-09-06 簽收**，套用至全站（海報掛牆 hero、IP 三聯畫、版次卡、溯源印章）。規格書見 `design/art-direction-v1.md`，視覺預覽見 `design/preview.html`。

## 本機預覽
在 `SILKY ART/` 目錄下：`python -m http.server 8091` → 瀏覽器看 `http://localhost:8091/`（英文主站）、`http://localhost:8091/zh/`（中文版）。

## 檔案結構
```
SILKY ART/
├ index.html          首頁（英文 · 主站）
├ exhibitions.html    策展與展覽（英文）
├ works.html          絲綢版次（英文）
├ provenance.html     溯源與評級（英文）
├ artists.html        藝術家（英文，含 #apply 投稿表）
├ publications.html   出版（英文）
├ about.html          關於中心（英文）
├ zh/                 中文版（同上七頁）
├ assets/             共用 css/js/海報 webp
├ CNAME               silklyart.com（GitHub Pages 自訂網域用）
├ design/             設計稿與規格書（參考，不上線也無妨）
└ 上線說明.md
```

## 投稿表怎麼收件
`artists.html` 與 `zh/artists.html` 的表單送出後，會開啟訪客郵件客戶端、預先寫好給 `seanown@gmail.com` 的信。**零設定、立刻能用**，但訪客需有郵件客戶端。上線後建議改為 Netlify Forms / Formspree。

## 上線部署（GitHub Pages，免費）
本站已 `git init`、commit 完成，remote 指向 `https://github.com/seanown/silklyart.git`，**只差 repo 還沒建**。
> ⚠️ **建庫這一步必須由軒哥在 github.com 做**：AI 連接器的 GitHub 權限是 403，無法建庫；但建立後本機 git push 可用，後續我會直接推。

1. **建立空 repo**：github.com → 右上角 **＋ → New repository**
   - Repository name：`silklyart`
   - 設為 **Public**
   - **不要**勾選 Add README / .gitignore / License（保持空庫，push 才不會衝突）
   - 按 **Create repository**
2. **推上線**（建好告訴我，我執行）：`git push -u origin main`
3. **開啟 Pages**：repo → Settings → Pages → Source 選 **Deploy from a branch** → Branch 選 **main** / 資料夾 **/(root)** → Save。
4. **自訂網域**：同一頁 Custom domain 填 `silklyart.com`（CNAME 檔已自帶，會自動帶出）→ Save；幾分鐘後勾選 **Enforce HTTPS**。

## 綁定網域 silklyart.com（name.com）
網域已在 name.com 購買、WHOIS 設 Private。GitHub Pages 用**頂級網域（apex）**，到 name.com 加 4 筆 **A 紀錄**指向 GitHub Pages：
```
185.199.108.153
185.199.109.153
185.199.110.153
185.199.111.153
```
（若要 `www.silklyart.com`，另加 CNAME → `seanown.github.io`。）DNS 生效後再於 Pages 設定勾 Enforce HTTPS。

## 上線前待補（非阻塞）
- 作品圖：目前用色塊佔位，待實際絲綢海報圖檔。
- 展覽檔期：目前是「策展方向」，待確定檔期再填。
- 價格：依鐵律 #1，等萬事利織造報價到手才公告。
- 投稿表：上線後建議換 Netlify Forms / Formspree。

## 對外紅線（已自檢通過）
不點名 萬事利 / IECIA / FORBES / Funko；南天門等中華神祇與館藏 IP 寫「授權」非「擁有」；
不寫價格；不提世界榜單平台（商會另一團隊）；禁用「絲綢比紙耐久／呼吸的紙」。
