# SILKY ART 靜態展示站 — 上線說明

> **雙語結構（2026-09-06 新增）**：中文站位於根目錄（`index.html` 等），全英文站位於 `en/`（`en/index.html` 等）。兩邊共用 `assets/css/style.css` 與各自 `assets/js/main.js`，頂部有「中文 / EN」切換。英文為優先上線語言——`silklyart.com` 可指向 `en/` 為主站，或加一頁語言 landing。

> 本資料夾是 **純靜態展示站**（HTML + CSS + 極少量 JS），無後端、無資料庫。
> 目的：先上線收藝術家投件、講清楚定位，**不顯價格**、不碰後端。
> ✅ **Mondo 風藝術指導 v1 已於 2026-09-06 簽收**，並套用至中文站＋英文站全頁（海報掛牆 hero、IP 三聯畫、版次卡、溯源印章）。規格書見 `design/art-direction-v1.md`，視覺預覽見 `design/preview.html`。

## 本機預覽
在 `SILKY ART/` 目錄下任一道指令：
- `python -m http.server 8081` → 開瀏覽器看 `http://localhost:8081/`
- 或直接雙擊 `index.html`（字體需聯網才會載入）

## 檔案結構
```
SILKY ART/
├ index.html          首頁（定位宣言＋六區塊）
├ exhibitions.html    策展與展覽
├ works.html          絲綢版次（系列容器＋登記意向）
├ provenance.html     溯源與評級
├ artists.html        藝術家（含 #apply 投稿表）
├ publications.html   出版
├ about.html          關於中心
├ assets/css/style.css  共用樣式（套規格書令牌）
├ assets/js/main.js     手機選單＋投稿表 mailto
└ 上線說明.md
```

## 投稿表現在怎麼收件
`artists.html` 的表單提交後，會開啟訪客的郵件客戶端、預先寫好給 `seanown@gmail.com` 的信。
**零設定、立刻能用**，但訪客必須有郵件客戶端。

## 上線部署（三選一）
1. **Netlify（推薦）**：把本資料夾拖到 Netlify Drop，或把程式碼 push 到 GitHub `seanown/longyousilk` 後用 Netlify Import from Git。
   - 部署後可把投稿表改成 **Netlify Forms**：在 `<form>` 加 `netlify` 屬性，訪客不必開郵件客戶端也能投件。
2. **GitHub Pages（免費）**：push 到 `seanown/longyousilk`，開啟 Pages，自訂網域設 `silklyart.com`。
3. **name.com 自家主機**：若 name.com 提供靜態託管，直接上傳本資料夾。

## 綁定網域 silklyart.com
網域已在 name.com 購買、WHOIS 設為 Private。部署平台給出 DNS 後，到 name.com 把 `silklyart.com` 指向該平台（A 紀錄或 NS 改用平台 DNS）。

## 上線前待補（非阻塞）
- 作品圖：目前用色塊佔位，待實際絲綢海報圖檔。
- 展覽檔期：目前是「策展方向」，待確定檔期再填。
- 價格：依鐵律 #1，等萬事利織造報價到手才公告。
- 投稿表：上線後建議換 Netlify Forms / Formspree。

## 對外紅線（已自檢通過）
不點名 萬事利 / IECIA / FORBES / Funko；南天門等中華神祇與館藏 IP 寫「授權」非「擁有」；
不寫價格；不提世界榜單平台（商會另一團隊）；禁用「絲綢比紙耐久／呼吸的紙」。
