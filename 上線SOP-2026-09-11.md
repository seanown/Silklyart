# SILKLY ART 上線 SOP（2026-09-11 實測版）

> 這份取代舊的 `上線說明.md`（那份寫的是 GitHub Pages，**已過時**）。
> 現在的託管商是 **Netlify**，網域 DNS 也已經搬到 Netlify。

---

## 一、先講結論：**網站已經在線上了**

| 檢查項 | 實測結果（2026-09-11 20:2x） | 判定 |
|---|---|---|
| https://silklyart.com/ | HTTP 200，`Server: Netlify` | ✅ 上線中 |
| https://silklyart.com/mo/（繁中） | HTTP 200 | ✅ 上線中 |
| https://silklyart.com/zh/（簡中） | HTTP 200 | ✅ 上線中 |
| https://silklyart.com/works.html | HTTP 200 | ✅ 上線中 |
| DNS：silklyart.com A 紀錄 | `75.2.60.5`（Netlify 的 IP） | ✅ 指向 Netlify |
| DNS：www.silklyart.com CNAME | `silklyart.netlify.app` | ✅ 指向 Netlify |
| DNS 由誰管理 | **name.com**（NS = NS1BCP/NS2GLX/NS3CQZ/NS4KPX.NAME.COM） | ⚠️ 見下方說明 |
| HTTPS | `Strict-Transport-Security` 已開 | ✅ 憑證有效 |
| GitHub `main` vs 本機 | 都是 `760d726`，**完全同步** | ✅ 無待推內容 |
| 部署方式 | push 到 GitHub → Netlify 自動建置 | ✅ 已接通 |

**所以你現在不需要「讓它上線」，它需要的是「改完內容後把它推上去」。**

---

## 二、它的「後台」在哪裡？（兩個，不是一個）

### 🔴 先講清楚：**這個站沒有 CMS 後台**
它是純靜態站（HTML + CSS + 極少量 JS），**沒有資料庫、沒有登入頁、沒有可以打字的編輯框**。
要改文案，就是改 HTML 檔。

### 你真正會用到的兩個後台

| 後台 | 網址 | 用來做什麼 |
|---|---|---|
| **Netlify 控制台**（主機後台） | https://app.netlify.com/sites/silklyart | 看部署紀錄、看上線狀態、回滾版本、管網域與 HTTPS、看流量 |
| **GitHub 倉庫**（程式碼後台） | https://github.com/seanown/Silklyart | 放檔案的地方；push 一次就觸發一次上線 |

> 站台名稱確認：`silklyart.netlify.app` 實測 200 → Netlify 站台 slug 就是 **silklyart**。

---

## 三、改一個字，到網友看得到：完整五步

```
① 改檔案（本機 C:\Users\user\WorkBuddy\SILKY ART\）
        ↓
② git add + commit（存一個版本）
        ↓
③ git push（丟上 GitHub）
        ↓
④ Netlify 自動收到通知 → 自動建置（約 10–40 秒）
        ↓
⑤ 自動上線，silklyart.com 就是新版
```

### 白話版
**GitHub 是倉庫，Netlify 是搬運工。** 你把貨放進倉庫（push），搬運工自動搬到店面（上線）。
你不用打電話叫它搬，它自己會去看。

---

## 四、兩條路，選一條

### 🅰️ 叫 AI 做（推薦，你只要動一張嘴）

你只要跟我說一句：

> 「SILKY ART 改好了，可以上線」

我就會做下面這串（你不用記）：

```bash
cd "C:/Users/user/WorkBuddy/SILKY ART"
git add -A
git commit -m "update: <改了什麼>"
git push origin main
```

然後我會回你：commit 編號 + 「約 1 分鐘後 https://silklyart.com/ 生效」。

> 🔴 我的規矩：**你沒說「上線」，我就不 push。** 這條不會變。

### 🅱️ 自己動手（學一次就會）

1. 用 VS Code / 記事本改 `C:\Users\user\WorkBuddy\SILKY ART\` 裡的檔案
2. 開 Git Bash 或終端機，依序貼三行：

```bash
cd "C:/Users/user/WorkBuddy/SILKY ART"
git add -A && git commit -m "改了什麼"
git push
```

3. 開 https://app.netlify.com/sites/silklyart/deploys 看有沒有出現一筆「Published」

> ⚠️ **三語鐵律**：改英文（根目錄）→ 一定要同步改 `mo/`（繁中）→ 再同步 `zh/`（簡中）。
> 只改一邊 = 三語內容不一致，這是這個站最容易出的錯。

---

## 五、常見狀況怎麼辦

| 狀況 | 怎麼處理 |
|---|---|
| 改了、push 了，但網站沒變 | 開 Netlify → **Deploys** 看最新一筆是不是 `Published`。若顯示 `Failed`，點進去看紅字 log，截圖給我 |
| 想回到上一版 | Netlify → **Deploys** → 找到舊的那筆 → 右上角 **Publish deploy**（不用改程式碼，30 秒回得去） |
| 想先預覽再決定要不要上線 | 開一個新分支 push 上去 → Netlify 會給一個 **Deploy preview** 網址，確認滿意再合併回 main |
| 想換網域 / 加 www | Netlify → **Domain management** → Add domain；**但改 DNS 紀錄要去 name.com**（NS 還在 name.com，只是 A 紀錄指向 Netlify） |

> ⚠️ **2026-09-19 更正**：本文件舊版寫「DNS 已經搬到 Netlify」是錯的。
> 實測 nameserver 仍是 name.com 的四台，Netlify 只是被 A 紀錄指過去。
> 影響：HTTPS 憑證 Netlify 仍會自動續（已開 HSTS，正常），但**任何 DNS 紀錄異動都要回 name.com 改**。
| 想看多少人來 | Netlify → **Analytics**（免費方案有基本數據） |

---

## 六、三條安全規則

1. 🔴 **main 分支 = 線上。** 任何 push 到 main 的東西，一分鐘內全世界看得到。
2. **大改動先開分支** → 用 Deploy preview 看過 → 再合回 main。
3. **上線前說一聲。** 不管是你要我推、還是你自己推，養成「先講再推」的習慣，
   萬一推錯，Netlify 的回滾只要 30 秒。

---

## 七、目前還沒做的三件事（不阻塞上線，但要排）

| # | 事項 | 說明 |
|---|---|---|
| 1 | 🔴 **沒有 CMS 後台** | 現在改文案要動 HTML。若你想「自己上網打字就能改」，要另外做（Netlify CMS / Decap 或簡易 admin），這是另一個工 |
| 2 | **Netlify CLI 未登入本機** | 本機沒有 `~/.netlify` 憑證，所以我目前只能用 `git push` 部署，不能用 `netlify deploy --prod` 直接推。要開的話要你本人跑一次 `npx netlify login` 點授權 |
| 3 | **投稿表是 mailto** | `artists.html` 的表單是開訪客的郵件軟體寄給 seanown@gmail.com。手機訪客可能打不開 → 建議換 Netlify Forms（原生支援，不用加後端） |

---

## 八、舊文件要刪

`上線說明.md` 裡面寫的「GitHub Pages + 4 筆 A 紀錄 185.199.x.x」**全部過時**，
CNAME 檔也已經在 commit `a3c6220` 移除（DNS 已遷到 Netlify）。
→ 建議下次 push 時把那份一起改掉，避免未來照著做做錯。

---

## 九、🔴 網域邊界（2026-09-19 軒哥確認，這節是鐵律）

### 唯一官方網域＝ `silklyart.com`

| 網域 | 是誰的 | 狀態 | 怎麼用 |
|---|---|---|---|
| **silklyart.com** | ✅ 軒哥（Name.com） | 正常營運、HTTPS、WHOIS 隱私已開 | ✅ **唯一對外網域**。名片、文宣、社群、EMAIL 簽名全部只用這個 |
| **silkly.com** | ❌ 第三方（GoDaddy） | 🔴 掛牌出售中（跳 `forsale.godaddy.com`），四項鎖全上 | 🚫 **不是我們的。不用、不連、不印** |

### silkly.com 實測紀錄（2026-09-19）

```
註冊商 : GoDaddy        註冊日 : 2016-08-07（十年前的舊域名）
NS     : Afternic（GoDaddy 旗下域名交易平台）
內容   : 114 bytes 停放頁 → 跳 https://forsale.godaddy.com/forsale/silkly.com
狀態   : 刪除／續約／轉移／更新 四項全鎖
```

### 三條硬規矩

1. 🚫 **任何文宣、名片、社群簡介、EMAIL 簽名，一律只寫 `silklyart.com`。**
   絕對不要出現 `silkly.com` —— 訪客點進去會看到「此域名出售」。
2. 🚫 **網站內不得建立任何指向 `silkly.com` 的連結**（2026-09-19 全站掃描：0 處，保持這樣）。
3. ✅ **品牌名統一寫 `SILKLY ART`**（兩個字中間一個空格）。
   網址與檔名維持小寫 `silklyart`，這是技術慣例，不要改。

### silklyart.com 續約提醒

| 項目 | 值 |
|---|---|
| 註冊日 | 2026-09-05 |
| **到期日** | **2027-09-05** |
| 鎖定狀態 | `client transfer prohibited`（正常防盜轉鎖） |
| WHOIS 隱私 | ✅ 已開（個資未公開，符合軒哥偏好） |

> 🔔 **建議去 name.com 開自動續約（Auto-renew）**。
> 這個網域是全站唯一的門牌，忘記續約被別人撿走，損失遠大於一年的註冊費。
